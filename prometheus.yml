global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'express-app'
    static_configs:
      - targets: ['app:3000']
    metrics_path: '/metrics'
